# Схема Запуска Сервисов WMSDIPL

```
┌─────────────────────────────────────────────────────────────────┐
│                      WMSDIPL - Архитектура                      │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐
│   PostgreSQL     │  ← Docker контейнер
│   Port: 55432    │     docker compose up -d postgres
└────────┬─────────┘     или: start-postgres.bat
         │
         │ JDBC Connection
         │
┌────────┴─────────┐
│    Core API      │  ← Spring Boot REST API
│   Port: 8080     │     gradle :core-api:bootRun
└────┬─────────┬───┘     или: start-core-api.bat
     │         │
     │         │ REST API
     │         │
     │    ┌────┴──────────────┐
     │    │  Import Service   │  ← Spring Boot Service
     │    │   Port: 8090      │     gradle :import-service:bootRun
     │    └───────────────────┘     или: start-import-service.bat
     │            │
     │            │ File System
     │            │
     │    ┌───────┴──────────┐
     │    │   input/ folder  │  ← XML файлы для импорта
     │    └──────────────────┘     Автоматически обрабатываются каждые 2 сек
     │
     │ REST API
     │
┌────┴─────────────┐
│  Desktop Client  │  ← JavaFX приложение
│   (JavaFX UI)    │     gradle :desktop-client:run
└──────────────────┘     или: start-desktop-client.bat
```

---

## Способы Запуска

### 🚀 Быстрый Запуск (Рекомендуется)

```cmd
start-all.bat
```

**Что делает**:
1. ✓ Проверяет PostgreSQL (запускает если нужно)
2. ✓ Запускает Core API (порт 8080) - ждет 10 сек
3. ✓ Запускает Import Service (порт 8090) - ждет 5 сек
4. ✓ Запускает Desktop Client
5. ✓ Все в отдельных окнах с логами

---

### 🔧 Ручной Запуск (По порядку)

```cmd
# Шаг 1: PostgreSQL (обязательно первым)
start-postgres.bat
# Ждать: 5 секунд

# Шаг 2: Core API (обязательно вторым)
start-core-api.bat
# Ждать: 10-15 секунд (пока Spring Boot запустится)

# Шаг 3: Import Service (опционально)
start-import-service.bat
# Ждать: 5 секунд

# Шаг 4: Desktop Client
start-desktop-client.bat
```

---

### 🎯 Выборочный Запуск

**Разработка Core API**:
```cmd
start-postgres.bat
start-core-api.bat
# Тестировать через curl или Postman
```

**Разработка Desktop Client**:
```cmd
start-postgres.bat
start-core-api.bat
start-desktop-client.bat
```

**Тестирование импорта**:
```cmd
start-postgres.bat
start-core-api.bat
start-import-service.bat
# Копировать XML файлы в input/
```

**Полное окружение**:
```cmd
start-all.bat
```

---

## Порты и URL

| Сервис          | Порт  | URL                              | Команда                    |
|-----------------|-------|----------------------------------|----------------------------|
| PostgreSQL      | 55432 | jdbc:postgresql://localhost:55432/wmsdipl | start-postgres.bat |
| Core API        | 8080  | http://localhost:8080            | start-core-api.bat         |
| Import Service  | 8090  | http://localhost:8090            | start-import-service.bat   |
| Desktop Client  | -     | JavaFX окно                      | start-desktop-client.bat   |

---

## Зависимости Сервисов

```
Desktop Client ──┐
                 ├──> Core API ──> PostgreSQL
Import Service ──┘
```

**Обязательный порядок**:
1. PostgreSQL (база данных)
2. Core API (зависит от PostgreSQL)
3. Import Service / Desktop Client (зависят от Core API)

---

## Остановка Сервисов

### 🛑 Быстрая Остановка Всех

```cmd
stop-all.bat
```

**Что делает**:
- Останавливает Core API (порт 8080)
- Останавливает Import Service (порт 8090)
- Останавливает Desktop Client (Java процессы)
- **НЕ** останавливает PostgreSQL (остается запущенным)

---

### 🔧 Ручная Остановка

**Core API**:
```cmd
netstat -ano | findstr :8080
taskkill /F /PID <PID>
```

**Import Service**:
```cmd
netstat -ano | findstr :8090
taskkill /F /PID <PID>
```

**PostgreSQL**:
```cmd
docker compose down
```

---

## Проверка Статуса

### ✓ Все сервисы разом

```cmd
# Core API
curl http://localhost:8080/actuator/health

# Import Service
curl http://localhost:8090/actuator/health

# PostgreSQL
docker ps | findstr wmsdipl-postgres

# Порты
netstat -ano | findstr :8080
netstat -ano | findstr :8090
netstat -ano | findstr :55432
```

---

## Логи и Отладка

| Сервис         | Где смотреть логи                  |
|----------------|------------------------------------|
| Core API       | Окно консоли Core API              |
| Import Service | Окно консоли Import Service        |
| Desktop Client | Файл `DEBUG.txt` в корне проекта   |
| PostgreSQL     | `docker logs wmsdipl-postgres`     |

---

## Тестовые Данные

После запуска `start-all.bat` доступны:

**Пользователь**:
- Username: `testuser`
- Password: `password`

**Задания**:
- Task #3: 0/50 (SKU-TEST-001)
- Task #4: 75/200 (SKU-TEST-002)

**Паллеты**:
- PLT-TEST-001, PLT-TEST-002, PLT-TEST-003, PLT-TEST-004, PLT-TEST-005

**Поступления**:
- 3 тестовые накладные в статусе IN_PROGRESS

---

## Быстрая Диагностика

**Проблема**: Desktop Client не подключается
```cmd
# Решение: Проверить Core API
curl http://localhost:8080/actuator/health
# Если не работает - перезапустить
start-core-api.bat
```

**Проблема**: Порт уже занят
```cmd
# Решение: Остановить и перезапустить
stop-all.bat
start-all.bat
```

**Проблема**: Import Service не видит файлы
```cmd
# Решение: Создать папку input
mkdir input
copy docs\import-template.xml input\test.xml
# Проверить логи Import Service в окне консоли
```

---

## BAT Файлы в Проекте

```
E:\WMSDIPL\
├── start-all.bat              ← 🚀 ГЛАВНЫЙ - Запуск всех сервисов
├── stop-all.bat               ← 🛑 Остановка всех сервисов
│
├── start-postgres.bat         ← PostgreSQL (порт 55432)
├── start-core-api.bat         ← Core API (порт 8080)
├── start-import-service.bat   ← Import Service (порт 8090) ✨ НОВЫЙ
├── start-desktop-client.bat   ← Desktop Client (JavaFX)
│
├── start-environment.bat      ← (старый) Запуск окружения
└── stop-environment.bat       ← (старый) Остановка окружения
```

**Рекомендация**: Используйте `start-all.bat` вместо `start-environment.bat`

---

## Документация

| Файл                              | Описание                                |
|-----------------------------------|-----------------------------------------|
| `BAT_FILES_GUIDE.md`              | Полное руководство по BAT файлам        |
| `IMPORT_SERVICE_QUICKSTART.md`    | Быстрый старт Import Service            |
| `TERMINAL_USER_GUIDE.md`          | Руководство по терминалу приёмки        |
| `QUICKSTART_TERMINAL.md`          | Быстрый старт терминала (30 сек)        |
| `NEXT_STEPS.md`                   | План дальнейших действий                |
| `README.md`                       | Общая информация о проекте              |

---

## Типичные Сценарии

### Первый Запуск

```cmd
# 1. Запустить все
start-all.bat

# 2. Дождаться открытия всех окон (20-30 сек)

# 3. Открыть Desktop Client
# Логин: testuser / password

# 4. Проверить работу терминала
# Перейти в "Терминал" → Выбрать задание → Сканировать
```

### Разработка с Изменениями Кода

```cmd
# 1. Остановить все
stop-all.bat

# 2. Внести изменения в код

# 3. Пересобрать
gradle clean build

# 4. Запустить заново
start-all.bat
```

### Тестирование Import Service

```cmd
# 1. Запустить все
start-all.bat

# 2. Подготовить тестовый файл
copy docs\import-template.xml input\receipt-001.xml

# 3. Подождать 2-3 секунды

# 4. Проверить в Desktop Client
# Раздел "Поступления" → "Обновить"

# 5. Или через API
curl -u testuser:password http://localhost:8080/api/receipts
```

---

**Создано**: Январь 2026  
**Версия**: 1.0  
**Проект**: WMSDIPL
