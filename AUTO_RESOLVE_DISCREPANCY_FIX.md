# ✅ Финальное исправление: Диалог расхождений + Автоматическое разрешение

## Что изменено

### 1. ✅ Осветлен текст в диалоге подтверждения
**Было**: Темный текст (#808080) плохо читался на темном фоне  
**Стало**: Светлый текст (#E0E0E0) хорошо виден

**Изменения**:
```java
alert.getDialogPane().setStyle(
    "-fx-background-color: #1c1c1c; " +
    "-fx-text-fill: #E0E0E0;"
);

alert.getDialogPane().lookup(".content.label").setStyle(
    "-fx-text-fill: #E0E0E0; " +
    "-fx-font-size: 14px;"
);
```

### 2. ✅ Изменен текст диалога

**Было**:
```
"приход может перейти в статус 'PENDING_RESOLUTION' (требуется разрешение расхождений)"
```

**Стало**:
```
"расхождения будут одобрены и зафиксированы, задание будет завершено, 
приход автоматически примет фактическое количество"
```

### 3. ✅ Автоматическое разрешение UNDER_QTY расхождений

**Логика**: Когда пользователь подтверждает завершение задачи с недостачей, это означает что он **осознанно принял решение** завершить приемку с меньшим количеством.

**Изменения в `TaskLifecycleService.complete()`**:
```java
// Auto-resolve: user confirmed completion despite shortage
discrepancy.setResolved(true);
discrepancy.setComment("Shortage confirmed by operator during task completion. " +
    "Expected: " + qtyAssigned + ", Received: " + qtyDone);
```

**Теперь**:
- ✅ UNDER_QTY расхождение создается с флагом `resolved = true`
- ✅ Добавляется комментарий о подтверждении оператором
- ✅ Приход **НЕ переходит** в PENDING_RESOLUTION
- ✅ Приход переходит **сразу в ACCEPTED** (если все задачи завершены)

## 📊 Как это работает

### Сценарий: Приемка с недостачей

1. **Оператор сканирует меньше** чем ожидается (50 вместо 100)
2. **Нажимает "Завершить"**
3. **Desktop-client** проверяет: `qtyDone (50) < qtyAssigned (100)` → **true**
4. **Показывается диалог** с предупреждением (светлый текст, хорошо читается)
5. **Пользователь видит два варианта**:
   - "Подтвердить завершение" - принять недостачу
   - "Отменить и вернуться" - досканировать

6. **При подтверждении**:
   - Backend вызывает `TaskLifecycleService.complete()`
   - Обнаруживается UNDER_QTY
   - Создается Discrepancy с `resolved = true`
   - Помечается последний скан `discrepancy = true`
   - Задача → COMPLETED

7. **После завершения всех задач**:
   - `ReceivingWorkflowService.completeReceiving()` проверяет расхождения
   - Все расхождения **уже resolved**
   - Приход → **ACCEPTED** (не PENDING_RESOLUTION!)

### Сценарий: Отмена и досканирование

1. Оператор видит диалог
2. **Выбирает "Отменить"**
3. Возвращается к форме сканирования
4. Досканирует недостающие 50 шт
5. Нажимает "Завершить"
6. Диалог **НЕ появляется** (нет расхождений)
7. Задача сразу завершается

## 🧪 Тестирование

### Подготовка

**1. Перезапустите core-api** (уже запущен ✅)

**2. Перезапустите desktop-client**:
```bash
# Закройте текущий desktop-client
# Запустите:
gradle :desktop-client:run
```

### Тест 1: Подтверждение недостачи

1. Войдите (admin/password)
2. Найдите RCP-NEW-SKU-001 (DRAFT)
3. Подтвердите → Начать приемку
4. Терминал → откройте задачу → Назначить → Начать
5. Отсканируйте **50 шт** (вместо 100)
6. Нажмите "Завершить"

**Ожидается**:
- ✅ Появится диалог со **светлым текстом** (хорошо читается)
- ✅ Текст: "расхождения будут одобрены и зафиксированы..."
- ✅ При подтверждении → задача завершается
- ✅ Таблица сканов обновляется → "⚠ Есть расхождение"

7. Завершите остальные задачи (можно просто подтвердить или правильно отсканировать)

8. Проверьте статус прихода в списке "Приходы"

**Ожидается**:
- ✅ Статус: **ACCEPTED** (не PENDING_RESOLUTION!)

### Проверка в БД

```sql
docker exec -i wmsdipl-postgres psql -U wmsdipl -d wmsdipl

-- Проверить расхождение
SELECT type, qty_expected, qty_actual, resolved, comment 
FROM discrepancies 
WHERE receipt_id = 28;

-- Должно показать:
-- type = 'UNDER_QTY'
-- resolved = TRUE (автоматически разрешено!)
-- comment = 'Shortage confirmed by operator during task completion...'

-- Проверить статус прихода
SELECT doc_no, status FROM receipts WHERE id = 28;

-- status должен быть ACCEPTED (не PENDING_RESOLUTION)
```

### Тест 2: Отмена и досканирование

1. Откатите приход:
```sql
DELETE FROM scans WHERE task_id IN (SELECT id FROM tasks WHERE receipt_id = 28);
DELETE FROM tasks WHERE receipt_id = 28;
DELETE FROM discrepancies WHERE receipt_id = 28;
DELETE FROM pallets WHERE receipt_id = 28;
UPDATE receipts SET status = 'DRAFT' WHERE id = 28;
```

2. Повторите приемку, но отсканируйте **50 шт**
3. Нажмите "Завершить"
4. **Выберите "Отменить и вернуться к приёмке"**
5. Досканируйте еще **50 шт** (итого 100)
6. Нажмите "Завершить"

**Ожидается**:
- ✅ Диалог **НЕ появляется** (нет расхождений)
- ✅ Задача сразу завершается
- ✅ В таблице сканов 2 записи, обе "✓ ОК"

## 📝 Измененные файлы

### Backend
- **`core-api/src/main/java/com/wmsdipl/core/service/TaskLifecycleService.java`**
  - UNDER_QTY расхождение создается с `resolved = true`
  - Добавлен комментарий о подтверждении оператором

### Frontend
- **`desktop-client/src/main/java/com/wmsdipl/desktop/DesktopClientApplication.java`**
  - Осветлен текст в диалоге (#E0E0E0)
  - Изменен текст о PENDING_RESOLUTION на автоматическое разрешение

## 🔍 Важные детали

### Почему UNDER_QTY auto-resolved?

**Логика**: Если оператор нажал "Подтвердить завершение" после предупреждения о недостаче, это означает:
1. Он **видел** диалог с предупреждением
2. Он **осознанно выбрал** продолжить несмотря на расхождение
3. Он **подтвердил**, что принятое количество корректно

Поэтому расхождение сразу помечается как разрешенное.

### Другие типы расхождений (OVER_QTY, BARCODE_MISMATCH)

**Вопрос**: Нужно ли их тоже auto-resolve?

**Текущая логика**:
- OVER_QTY - создается во время скана, **НЕ auto-resolved**
- BARCODE_MISMATCH - создается во время скана, **НЕ auto-resolved**
- UNDER_QTY - создается при завершении, **auto-resolved**

Если нужно auto-resolve и другие типы, можно добавить логику в `ReceivingWorkflowService.recordScan()`.

## 🎯 Следующие шаги (опционально)

1. **Auto-resolve все расхождения** при подтверждении диалога
2. Добавить **отдельный экран** для просмотра расхождений
3. Добавить **отчет** по расхождениям

## ⚡ Статус

- ✅ Backend скомпилирован и запущен
- ✅ Frontend скомпилирован
- ⏳ Desktop-client нужно перезапустить
- ✅ Тестовые данные готовы

**ГОТОВО К ТЕСТИРОВАНИЮ!** 🚀

Дата: 08 января 2026
