# ✅ ФИНАЛЬНОЕ ИСПРАВЛЕНИЕ: PENDING_RESOLUTION больше не появляется!

## ❌ Проблема

После подтверждения расхождения в диалоге приход все равно переходил в **PENDING_RESOLUTION**, хотя:
- ✅ Диалог появлялся
- ✅ Пользователь подтвердил расхождение
- ✅ Расхождение создано с `resolved = true`

**Причина**: Метод `ReceivingWorkflowService.completeReceiving()` проверял **количественный разрыв** (`hasQtyGap`), независимо от статуса расхождения:

```java
// СТАРАЯ ЛОГИКА ❌
boolean hasQtyGap = tasks.stream()
    .anyMatch(t -> t.getQtyDone() < t.getQtyAssigned());

boolean hasOpenDiscrepancy = discrepancies.stream()
    .anyMatch(d -> !d.getResolved());

// Если ЛЮБОЕ из условий true → PENDING_RESOLUTION
if (hasOpenDiscrepancy || hasQtyGap) {
    receipt.setStatus(PENDING_RESOLUTION);
}
```

**Проблема**: Даже если расхождение `resolved = true`, но есть `qtyGap` → статус PENDING_RESOLUTION.

## ✅ Решение

**Убрали проверку `hasQtyGap`**. Теперь проверяем **только наличие неразрешенных расхождений**:

```java
// НОВАЯ ЛОГИКА ✅
// Проверяем только НЕРАЗРЕШЕННЫЕ расхождения
boolean hasOpenDiscrepancy = discrepancies.stream()
    .anyMatch(d -> d.getResolved() == null || !d.getResolved());

if (hasOpenDiscrepancy) {
    receipt.setStatus(PENDING_RESOLUTION);
} else {
    receipt.setStatus(ACCEPTED);  // Все расхождения resolved → ACCEPTED
}
```

**Обоснование**:
- Если оператор **подтвердил** недостачу в диалоге → расхождение создается с `resolved = true`
- Значит, недостача **одобрена** и не требует дальнейшего разрешения
- Приход может сразу перейти в **ACCEPTED**

## 📊 Логика работы

### До исправления ❌

```
1. Оператор отсканировал 50 из 100
2. Подтвердил завершение в диалоге
3. Backend создал UNDER_QTY с resolved=true
4. completeReceiving() проверяет:
   - hasOpenDiscrepancy = false (resolved=true)
   - hasQtyGap = true (50 < 100)
5. if (false || true) → PENDING_RESOLUTION ❌
```

### После исправления ✅

```
1. Оператор отсканировал 50 из 100
2. Подтвердил завершение в диалоге
3. Backend создал UNDER_QTY с resolved=true
4. completeReceiving() проверяет:
   - hasOpenDiscrepancy = false (resolved=true)
   - hasQtyGap = (УБРАНО!)
5. if (false) → ACCEPTED ✅
```

## 🧪 Тестирование

### Подготовка

**Core-API перезапускается** (подождите ~30 секунд после запуска скрипта)

**Desktop-client** можно оставить как есть (изменения только в backend)

### Тест: Полный цикл с подтверждением недостачи

1. **Войдите**: admin / password
2. **Найдите**: RCP-NEW-SKU-001 (DRAFT)
3. **Подтвердите** → "Начать приемку"
4. **Терминал** → откройте задачу #1
5. **Назначить** → **Начать**
6. **Отсканируйте недостачу**:
   - Паллета: `PLT-001`
   - Количество: **50** (меньше чем 100)
   - "Записать скан"
7. **Нажмите "Завершить"**
8. **Диалог появится** → нажмите **"Подтвердить завершение"**
9. Задача #1 → COMPLETED

10. **Завершите остальные задачи**:
    - Задача #2: отсканируйте точное количество (50.5)
    - Задача #3: отсканируйте точное количество (25)
    - Или просто назначьте и завершите без сканирования (0 < assigned → снова появится диалог)

11. **После завершения всех 3 задач**

### ✅ Ожидаемый результат

**В списке "Приходы":**
- ✅ Статус: **ACCEPTED** (не PENDING_RESOLUTION!)

**В базе данных:**
```sql
SELECT doc_no, status FROM receipts WHERE id = 28;
-- status = 'ACCEPTED'

SELECT type, resolved FROM discrepancies WHERE receipt_id = 28;
-- type = 'UNDER_QTY', resolved = TRUE
```

### Проверка в БД

```sql
docker exec -i wmsdipl-postgres psql -U wmsdipl -d wmsdipl

-- Проверить статус прихода
SELECT doc_no, status FROM receipts WHERE id = 28;

-- Должно показать:
-- status = 'ACCEPTED' (НЕ 'PENDING_RESOLUTION')

-- Проверить расхождения
SELECT id, type, qty_expected, qty_actual, resolved, comment 
FROM discrepancies 
WHERE receipt_id = 28;

-- Должно показать:
-- type = 'UNDER_QTY'
-- resolved = TRUE
-- comment = 'Shortage confirmed by operator during task completion...'
```

## 📝 Измененные файлы

### Backend
**`core-api/src/main/java/com/wmsdipl/core/service/workflow/ReceivingWorkflowService.java`**

**Метод `completeReceiving()`**:
- ❌ Удалена проверка `hasQtyGap`
- ✅ Проверяется только `hasOpenDiscrepancy` (resolved = false или null)
- ✅ Если все расхождения resolved → ACCEPTED
- ✅ Если есть unresolved → PENDING_RESOLUTION

## 🎯 Итоговое поведение

### Сценарий 1: Недостача с подтверждением
```
1. Отсканировано 50 из 100
2. Нажата кнопка "Завершить"
3. Диалог показывается ✅
4. Пользователь подтверждает
5. Расхождение: UNDER_QTY, resolved=true
6. Задача: COMPLETED
7. После всех задач: Приход → ACCEPTED ✅
```

### Сценарий 2: Превышение с подтверждением
```
1. Отсканировано 150 из 100
2. Скан помечен discrepancy=true (OVER_QTY)
3. Нажата кнопка "Завершить"
4. Диалог показывается (hasScanDiscrepancies=true)
5. Пользователь подтверждает
6. Задача: COMPLETED
7. Но OVER_QTY не auto-resolved → PENDING_RESOLUTION
```

**Вопрос**: Нужно ли также auto-resolve OVER_QTY при подтверждении?

### Сценарий 3: Приемка без расхождений
```
1. Отсканировано 100 из 100 (точно)
2. Нажата кнопка "Завершить"
3. Диалог НЕ показывается (нет расхождений)
4. Задача сразу: COMPLETED
5. Приход: ACCEPTED
```

## 🔍 Дополнительная логика (опционально)

### Вопрос: Что делать с OVER_QTY, BARCODE_MISMATCH, SSCC_MISMATCH?

**Текущая логика**:
- **UNDER_QTY** (создается при завершении) → auto-resolved ✅
- **OVER_QTY** (создается при скане) → НЕ auto-resolved ❌
- **BARCODE_MISMATCH** (создается при скане) → НЕ auto-resolved ❌
- **SSCC_MISMATCH** (создается при скане) → НЕ auto-resolved ❌

**Если нужно auto-resolve все типы расхождений при подтверждении диалога**:
1. Передавать флаг `userConfirmed=true` в метод `completeTask()`
2. В `ReceivingWorkflowService.recordScan()` не auto-resolve
3. После `completeTask()` на клиенте вызывать `auto-resolveAllDiscrepancies(taskId)`

Или проще: при подтверждении диалога разрешать все расхождения задачи автоматически.

## ⚡ Статус

- ✅ Backend исправлен и скомпилирован
- ⏳ Core-API перезапускается (~30 секунд)
- ✅ Desktop-client без изменений (можно не перезапускать)
- ✅ Тестовые данные готовы (приход RCP-NEW-SKU-001 в DRAFT)

## 📄 Связанные документы

- `CRITICAL_DIALOG_FIX.md` - исправление появления диалога
- `AUTO_RESOLVE_DISCREPANCY_FIX.md` - автоматическое разрешение UNDER_QTY
- `FINAL_UNDER_QTY_FIX_READY.md` - первое исправление

---

**ФИНАЛЬНОЕ ИСПРАВЛЕНИЕ ПРИМЕНЕНО!** 🎉  
**Приход теперь переходит в ACCEPTED при подтверждении расхождений!** ✅

Дата: 08 января 2026
