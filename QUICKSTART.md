# 🎯 БЫСТРЫЙ СТАРТ - Тестирование Терминала

## ✅ Окружение готово!

### Запущенные сервисы:
- ✅ PostgreSQL (порт 55432)
- ✅ Core API (порт 8080)
- ⏳ Desktop Client - нужно запустить вручно

---

## 🚀 Запуск Desktop Client

### Способ 1: Batch-файл
Дважды кликнуть на:
```
start-desktop-client.bat
```

### Способ 2: Командная строка
```bash
cd E:\WMSDIPL
gradle :desktop-client:run
```

Подождать ~20 секунд пока откроется окно JavaFX.

---

## 🔐 Учетные данные

```
Username: testuser
Password: password
```

**Важно:** Если не подошли, попробуйте:
```
Username: admin
Password: admin
```

---

## 📝 Что делать после входа

### 1. Открыть терминал
- Нажать кнопку **"Терминал"** в левой панели
- Увидите таблицу "Мои задачи"

### 2. Проверить задания
Должно быть **2 задания**:
- Задание #3: ACT-TEST-2026-001 (0/50)
- Задание #4: ACT-TEST-2026-002 (75/200)

### 3. Начать сканирование
1. **Дважды кликнуть** на задание
2. Откроется диалог с табами "Документ" и "Факт"
3. В табе **"Факт"** заполнить:
   - Паллета: `PLT-TEST-001`
   - Баркод: `SKU-TEST-002`
   - Количество: `10`
4. Нажать **Enter** или кнопку "Отправить"
5. Увидеть **зелёную вспышку** ✅
6. Запись появится в "История сканов"

---

## 🧪 Быстрый тест

### Тест 1: Успешный скан
```
Паллета:    PLT-TEST-001
Баркод:     SKU-TEST-002
Количество: 10
```
**Ожидание**: Зелёная анимация, скан записан

### Тест 2: Ошибка - пустая паллета
```
Паллета:    (оставить пустым)
Баркод:     SKU-TEST-002
Количество: 10
```
**Ожидание**: Красная тряска, сообщение об ошибке

---

## 🔧 Если что-то не работает

### Desktop Client не запускается
```bash
# Проверить, что Core API работает:
curl http://localhost:8080/actuator/health

# Должен вернуть: {"status":"UP"}
```

### Не вижу заданий
1. Проверить, что вошли как `testuser`
2. Нажать кнопку "Обновить"
3. Проверить в БД:
```bash
docker exec -it wmsdipl-postgres psql -U wmsdipl -d wmsdipl -c "SELECT * FROM tasks WHERE assignee='testuser';"
```

### Логин не подходит
Попробовать:
- `admin` / `admin`
- Или пересоздать пользователя:
```bash
docker exec -i wmsdipl-postgres psql -U wmsdipl -d wmsdipl -c "INSERT INTO users (username, password_hash, full_name, role, is_active) VALUES ('testuser', '{noop}password', 'Test User', 'OPERATOR', true) ON CONFLICT (username) DO UPDATE SET password_hash = '{noop}password';"
```

---

## 📋 Полная документация

- **Чеклист тестирования**: `docs/TERMINAL_TESTING_CHECKLIST.md`
- **Руководство**: `TESTING_GUIDE.md`
- **Статус окружения**: `READY_TO_TEST.md`

---

## 🎬 Готово!

**Запустите Desktop Client и начинайте тестирование!** 🚀

Логин: `testuser` / Пароль: `password`
