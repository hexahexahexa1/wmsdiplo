# ✅ ОКРУЖЕНИЕ ПОЛНОСТЬЮ ГОТОВО К ТЕСТИРОВАНИЮ!

## 🎉 Статус

### ✅ Запущено и работает:
1. **PostgreSQL** - База данных (контейнер wmsdipl-postgres)
2. **Core API** - REST API на http://localhost:8080
3. **Пользователь testuser** - Создан и проверен
4. **Тестовые данные** - Загружены (2 задания для testuser)

### ⏳ Требует ручного запуска:
- **Desktop Client** - JavaFX приложение

---

## 🚀 ЗАПУСТИТЬ DESKTOP CLIENT СЕЙЧАС

### Вариант 1: Batch-файл (рекомендуется)
**Дважды кликните** на файл в папке проекта:
```
start-desktop-client.bat
```

### Вариант 2: Командная строка
Откройте CMD и выполните:
```cmd
cd E:\WMSDIPL
gradle :desktop-client:run
```

**Подождите ~20 секунд** пока откроется окно приложения.

---

## 🔐 УЧЕТНЫЕ ДАННЫЕ (ПРОВЕРЕНО ✅)

```
Username: testuser
Password: password
```

**Статус**: Логин успешно проверен через API ✅

---

## 📊 ТЕСТОВЫЕ ДАННЫЕ (ГОТОВЫ ✅)

### Задания для пользователя testuser:

| ID | Статус | Приход | Ожидается | Выполнено | Прогресс |
|----|--------|--------|-----------|-----------|----------|
| **3** | IN_PROGRESS | ACT-TEST-2026-001 | 50 | 0 | 0 / 50 |
| **4** | IN_PROGRESS | ACT-TEST-2026-002 | 200 | 75 | 75 / 200 |

### Доступные ресурсы:

**Паллеты для сканирования:**
- `PLT-TEST-001` (пустая)
- `PLT-TEST-002` (пустая)
- `PLT-TEST-003` (используется, qty=75, SKU-TEST-002)
- `PLT-TEST-004` (пустая)
- `PLT-TEST-005` (пустая)

**Баркоды товаров:**
- `SKU-TEST-001`
- `SKU-TEST-002`
- `SKU-TEST-003`

---

## 🎯 ЧТО ДЕЛАТЬ ПОСЛЕ ВХОДА

### Шаг 1: Войти в систему
В окне Desktop Client ввести:
- Username: `testuser`
- Password: `password`

### Шаг 2: Открыть терминал
- Нажать кнопку **"Терминал"** в левой панели навигации

### Шаг 3: Увидеть список заданий
Должно отобразиться **2 задания** в таблице "Мои задачи"

### Шаг 4: Открыть задание
- **Дважды кликнуть** на любое задание (например, ID=3)

### Шаг 5: Начать сканирование
В табе **"Факт"**:
1. Паллета: `PLT-TEST-001` (Enter)
2. Баркод: `SKU-TEST-002` (Enter)
3. Количество: `10` (Enter - автоотправка)
4. Увидеть **зелёную вспышку успеха** ✅
5. Запись появится в таблице "История сканов"

---

## 🧪 БЫСТРЫЕ ТЕСТЫ

### ✅ Успешный скан
```
Задание: #3
Паллета: PLT-TEST-001
Баркод: SKU-TEST-002
Количество: 10
```
**Ожидание**: Зелёная анимация, поля очищаются, скан в истории

### ❌ Валидация - пустая паллета
```
Задание: #3
Паллета: (оставить пустым)
Баркод: SKU-TEST-002
Количество: 10
```
**Ожидание**: Красная тряска, сообщение "Код паллеты обязателен"

### ⚠️ Расхождение - неверный баркод
```
Задание: #3
Паллета: PLT-TEST-002
Баркод: WRONG-BARCODE
Количество: 5
```
**Ожидание**: Скан записан с отметкой "BARCODE_MISMATCH" в колонке "Расхождение"

### 🔄 Частично выполненное задание
```
Задание: #4 (уже 75/200)
Паллета: PLT-TEST-003
Баркод: SKU-TEST-002
Количество: 20
```
**Ожидание**: Прогресс обновится до 95/200

---

## 🔍 ПРОВЕРКА ОКРУЖЕНИЯ

### Проверить Core API:
```bash
curl http://localhost:8080/actuator/health
```
**Должен вернуть**: `{"status":"UP"}` ✅

### Проверить задания:
```bash
curl -u testuser:password http://localhost:8080/api/tasks
```
**Должен вернуть**: JSON массив с заданиями (минимум 2 для testuser) ✅

### Проверить PostgreSQL:
```bash
docker ps | grep postgres
```
**Должен показать**: контейнер wmsdipl-postgres (healthy) ✅

---

## 🛠️ ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Desktop Client не открывается
1. Проверить, что Core API запущен (см. выше)
2. Закрыть все Java процессы
3. Перезапустить: `start-desktop-client.bat`

### Логин не подходит
```bash
# Пересоздать пользователя:
docker exec -i wmsdipl-postgres psql -U wmsdipl -d wmsdipl -c "DELETE FROM users WHERE username='testuser'; INSERT INTO users (username, password_hash, full_name, role, is_active) VALUES ('testuser', '{noop}password', 'Test User', 'OPERATOR', true);"
```

### Не вижу заданий в терминале
1. Проверить, что вы вошли как `testuser` (не admin)
2. Нажать кнопку "Обновить"
3. Проверить в БД:
```bash
docker exec -it wmsdipl-postgres psql -U wmsdipl -d wmsdipl -c "SELECT id, assignee, status FROM tasks WHERE assignee='testuser';"
```

---

## 📚 ДОКУМЕНТАЦИЯ

- **Быстрый старт**: `QUICKSTART.md` (этот файл)
- **Полный чеклист**: `docs/TERMINAL_TESTING_CHECKLIST.md` (80+ тестов)
- **Руководство**: `TESTING_GUIDE.md`
- **SQL тестовых данных**: `test-data-final.sql`

---

## 🎬 ГОТОВО К ТЕСТИРОВАНИЮ!

**Запускайте Desktop Client прямо сейчас:**
```
Дважды кликнуть: start-desktop-client.bat
```

**Логин**: `testuser` / **Пароль**: `password`

**Удачи с тестированием!** 🚀
