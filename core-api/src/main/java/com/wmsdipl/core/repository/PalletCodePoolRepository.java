package com.wmsdipl.core.repository;

import com.wmsdipl.core.domain.PalletCodePool;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PalletCodePoolRepository extends JpaRepository<PalletCodePool, Long> {
}
