# ✅ Исправление: Паллеты теперь переходят в статус RECEIVED

## ❌ Проблема

После завершения всех задач приемки паллеты оставались в статусе **RECEIVING**, из-за чего невозможно было создать задачи на размещение.

**Ошибка**:
```
No placement tasks could be generated. 
Check that pallets are in RECEIVED status.
```

**Причина**: Метод `ReceivingWorkflowService.completeReceiving()` обновлял статус прихода на ACCEPTED, но **не обновлял статус паллет** с RECEIVING на RECEIVED.

## ✅ Решение

Добавлена логика перевода паллет в статус RECEIVED при успешном завершении приемки:

```java
if (hasOpenDiscrepancy) {
    receipt.setStatus(ReceiptStatus.PENDING_RESOLUTION);
} else {
    receipt.setStatus(ReceiptStatus.ACCEPTED);
    
    // Transition pallets from RECEIVING to RECEIVED
    // This allows placement tasks to be created
    List<Pallet> pallets = palletRepository.findByReceipt(receipt);
    pallets.stream()
        .filter(p -> p.getStatus() == PalletStatus.RECEIVING)
        .forEach(p -> {
            p.setStatus(PalletStatus.RECEIVED);
            palletRepository.save(p);
        });
}
```

## 📊 Жизненный цикл паллеты при приемке

### До исправления ❌
```
1. Создание паллеты: EMPTY
2. Первый скан: RECEIVING (pallet initialized)
3. Дополнительные сканы: RECEIVING (quantity accumulated)
4. Завершение задачи: RECEIVING (НЕ изменяется!)
5. Завершение прихода: RECEIVING (НЕ изменяется!)
6. Попытка создать задачи на размещение: ОШИБКА ❌
```

### После исправления ✅
```
1. Создание паллеты: EMPTY
2. Первый скан: RECEIVING (pallet initialized)
3. Дополнительные сканы: RECEIVING (quantity accumulated)
4. Завершение задачи: RECEIVING
5. Завершение прихода (ACCEPTED): RECEIVED ✅
6. Создание задач на размещение: SUCCESS
```

## 🔄 Полный workflow прихода с паллетами

### Статус прихода → Статус паллет

```
DRAFT
  ↓ confirmReceipt()
CONFIRMED
  ↓ startReceiving()
IN_PROGRESS
  ↓ (паллеты создаются при первом скане)
  | Pallet: RECEIVING (во время сканирования)
  ↓ completeReceiving()
ACCEPTED
  | Pallet: RECEIVED ✅ (готовы к размещению)
  ↓ startPlacement()
PLACING
  | Pallet: IN_TRANSIT (во время размещения)
  ↓ completePlacement()
STOCKED
  | Pallet: STORED (размещены на складе)
```

## 📝 Измененный файл

**`core-api/src/main/java/com/wmsdipl/core/service/workflow/ReceivingWorkflowService.java`**

**Метод `completeReceiving()`**:
- Добавлена логика обновления статуса паллет при переходе прихода в ACCEPTED
- Паллеты фильтруются по статусу RECEIVING
- Каждая паллета переводится в статус RECEIVED
- Паллеты сохраняются в БД

## 🧪 Тестирование

### Шаг 1: Перезапустите core-api

Core-API перезапускается (подождите ~30 секунд)

### Шаг 2: Проверьте существующие паллеты

```sql
docker exec -i wmsdipl-postgres psql -U wmsdipl -d wmsdipl

SELECT id, code, status, quantity FROM pallets WHERE receipt_id = 28;

-- Должно показать:
-- status = 'RECEIVED' (уже обновлено вручную)
```

### Шаг 3: Создайте задачи на размещение

**В desktop-client**:
1. Найдите приход RCP-NEW-SKU-001 (статус: ACCEPTED)
2. Нажмите кнопку "Начать размещение"

**Или через API**:
```bash
curl -X POST http://localhost:8080/api/receipts/28/start-placement \
  -u admin:password \
  -H "Content-Type: application/json"
```

**Ожидаемый результат**:
- ✅ Создано 3 задачи на размещение (по одной на каждую паллету)
- ✅ Статус прихода → PLACING
- ✅ Статус паллет → IN_TRANSIT

### Шаг 4: Проверьте в БД

```sql
-- Проверить задачи на размещение
SELECT id, task_type, status, pallet_id, target_location_id 
FROM tasks 
WHERE receipt_id = 28 AND task_type = 'PLACEMENT';

-- Должно показать 3 задачи типа PLACEMENT

-- Проверить статус паллет
SELECT id, code, status FROM pallets WHERE receipt_id = 28;

-- status = 'IN_TRANSIT'

-- Проверить статус прихода
SELECT doc_no, status FROM receipts WHERE id = 28;

-- status = 'PLACING'
```

## 🔍 Новый полный тест (с нуля)

### 1. Сбросить приход

```sql
DELETE FROM scans WHERE task_id IN (SELECT id FROM tasks WHERE receipt_id = 28);
DELETE FROM tasks WHERE receipt_id = 28;
DELETE FROM discrepancies WHERE receipt_id = 28;
DELETE FROM pallets WHERE receipt_id = 28;
UPDATE receipts SET status = 'DRAFT' WHERE id = 28;
```

### 2. Приемка

1. RCP-NEW-SKU-001 → Подтвердить → Начать приемку
2. Терминал → открыть задачу → Назначить → Начать
3. Отсканировать (можно с недостачей или точно)
4. Завершить (если недостача → подтвердить диалог)
5. Повторить для всех 3 задач

**Проверить**:
```sql
SELECT status FROM receipts WHERE id = 28;
-- status = 'ACCEPTED'

SELECT code, status FROM pallets WHERE receipt_id = 28;
-- status = 'RECEIVED' для всех паллет ✅
```

### 3. Размещение

1. Приход RCP-NEW-SKU-001 → "Начать размещение"

**Проверить**:
```sql
SELECT task_type, status FROM tasks WHERE receipt_id = 28 AND task_type = 'PLACEMENT';
-- Должно быть 3 задачи со статусом NEW

SELECT status FROM receipts WHERE id = 28;
-- status = 'PLACING'

SELECT code, status FROM pallets WHERE receipt_id = 28;
-- status = 'IN_TRANSIT'
```

### 4. Завершение размещения

1. Назначить и завершить все задачи PLACEMENT
2. Вызвать `POST /api/receipts/28/complete-placement`

**Проверить**:
```sql
SELECT status FROM receipts WHERE id = 28;
-- status = 'STOCKED'

SELECT code, status FROM pallets WHERE receipt_id = 28;
-- status = 'STORED'
```

## 🎯 Итоговая логика

### Когда паллеты меняют статус?

1. **EMPTY → RECEIVING**
   - При первом скане на паллету (`ReceivingWorkflowService.recordScan()`)
   - Паллета инициализируется: устанавливается SKU, receipt, location

2. **RECEIVING → RECEIVED**
   - При завершении приемки (`ReceivingWorkflowService.completeReceiving()`)
   - Условие: приход переходит в ACCEPTED (все расхождения resolved)

3. **RECEIVED → IN_TRANSIT**
   - При создании задач на размещение (`PutawayService.generatePlacementTasks()`)
   - Для каждой паллеты создается задача PLACEMENT

4. **IN_TRANSIT → STORED**
   - При завершении размещения (`PlacementWorkflowService.completePlacement()`)
   - После выполнения всех задач PLACEMENT

## ⚠️ Важные замечания

### Что если приход в PENDING_RESOLUTION?

Если приход перешел в PENDING_RESOLUTION (есть неразрешенные расхождения):
- Паллеты **остаются в RECEIVING**
- Задачи на размещение **создать нельзя**
- Нужно:
  1. Разрешить расхождения (POST /api/discrepancies/{id}/resolve)
  2. Повторить завершение приемки (POST /api/receipts/{id}/complete-receiving)
  3. Только после перехода в ACCEPTED паллеты → RECEIVED

### Ручное обновление существующих паллет

Для текущего прихода (id=28) паллеты были обновлены вручную:
```sql
UPDATE pallets 
SET status = 'RECEIVED' 
WHERE receipt_id = 28 AND status = 'RECEIVING';
```

Это нужно было сделать **один раз**, потому что логика не была реализована ранее.

**В будущем** (после перезапуска core-api с исправлением) паллеты будут переходить в RECEIVED **автоматически** при вызове `completeReceiving()`.

## ⚡ Статус

- ✅ Backend исправлен и скомпилирован
- ⏳ Core-API перезапускается (~30 секунд)
- ✅ Существующие паллеты обновлены вручную (готовы к размещению)
- ✅ Готово к тестированию создания задач на размещение

## 📄 Связанные документы

- `FINAL_PENDING_RESOLUTION_FIX.md` - исправление PENDING_RESOLUTION
- `CRITICAL_DIALOG_FIX.md` - исправление появления диалога
- `AUTO_RESOLVE_DISCREPANCY_FIX.md` - автоматическое разрешение расхождений

---

**ИСПРАВЛЕНИЕ ПРИМЕНЕНО!** 🎉  
**Паллеты теперь автоматически переходят в RECEIVED после приемки!** ✅

Дата: 08 января 2026
