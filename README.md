# WMSDIPL

Много-модульный проект для модуля приходных накладных склада.

## Модули
- core-api — Spring Boot REST API, Postgres, Flyway (накладные/статусы/идемпотентность messageId).
- import-service — watch-folder через планировщик (пуллинг папки) для импорта XML → REST в core-api.
- desktop-client — JavaFX настольный клиент (без веб-UI, по договорённости).

## Текущее состояние (desktop-client)
- **Экран приходов**: таблица с полями `docNo`, `messageId`, `status`, `docDate`, `supplier`. Двойной клик открывает детали строк.
- **Терминал приёмки** ✨ (НОВОЕ):
  - Список заданий с прогрессом (qtyDone / qtyAssigned)
  - Форма сканирования с автопереходом по Enter
  - Анимации успеха (зелёная вспышка) и ошибки (красная тряска)
  - Таблица истории сканов
  - Поддержка паллет и штрих-кодов
- Минималистичные скроллбары, навигация пилюлями, шрифты Montserrat.

## Текущее состояние (core-api/import-service)
- Flyway миграция V1 создаёт акты, строки, задания, сканы, расхождения, журнал импорта, историю статусов.
- Импорт XML (docs/import-template.xml) пишет акты в статус Draft, идемпотентность по messageId (уникальный индекс на receipts.message_id и import_log.message_id).
- Индексы по статусу на receipts и tasks для быстрых выборок.

## ER-диаграмма
- Исходник Mermaid: [docs/er-diagram.mmd](docs/er-diagram.mmd)
- PDF: [docs/er-diagram.pdf](docs/er-diagram.pdf)
- Основана на миграции [core-api/src/main/resources/db/migration/V1__init.sql](core-api/src/main/resources/db/migration/V1__init.sql)

## Требования
- Java 17
- Gradle 8+ (или добавьте wrapper)
- Docker (для Postgres; Keycloak секция закомментирована)

## Быстрый старт

### Автоматический запуск всего окружения (рекомендуется)
```cmd
start-all.bat
```
Автоматически запустит PostgreSQL, Core API, Import Service и Desktop Client в отдельных окнах.

### Ручной запуск отдельных сервисов
1. **PostgreSQL**: `docker compose up -d postgres` или `start-postgres.bat`
2. **Core API** (порт 8080): `start-core-api.bat` или `gradle :core-api:bootRun`
3. **Import Service** (порт 8090): `start-import-service.bat` или `gradle :import-service:bootRun`
4. **Desktop Client**: `start-desktop-client.bat` или `gradle :desktop-client:run`

### Остановка всех сервисов
```cmd
stop-all.bat
```

### Доступные BAT файлы
- `start-all.bat` — запуск всех сервисов автоматически
- `stop-all.bat` — остановка всех сервисов
- `start-postgres.bat` — только PostgreSQL
- `start-core-api.bat` — только Core API (порт 8080)
- `start-import-service.bat` — только Import Service (порт 8090)
- `start-desktop-client.bat` — только Desktop Client

### Вход в систему
- **Логин**: `testuser`
- **Пароль**: `password`

### Терминал приёмки
1. В главном меню нажать **"Терминал"**
2. Дважды кликнуть на задание
3. Вкладка **"Факт"** → сканировать товар

📖 **Полная инструкция**: [TERMINAL_USER_GUIDE.md](TERMINAL_USER_GUIDE.md)  
⚡ **Быстрый старт**: [QUICKSTART_TERMINAL.md](QUICKSTART_TERMINAL.md)

## Импорт XML
Шаблон файла: `docs/import-template.xml`. 

### Как импортировать файлы:
1. Запустите Import Service: `start-import-service.bat`
2. Положите XML файл в папку `input/` (по умолчанию)
3. Import Service автоматически обработает файл каждые 2 секунды
4. Сервис вычислит messageId (атрибут или SHA-256 файла) и вызовет `/api/imports` в Core API
5. Повторный файл с тем же messageId вернёт тот же результат (идемпотентность)

### Настройки Import Service:
- **Порт**: 8090 (по умолчанию)
- **Папка импорта**: `input/` (настраивается через `WMS_IMPORT_FOLDER`)
- **Интервал проверки**: 2000 мс (настраивается через `WMS_IMPORT_POLL_MS`)
- **Core API URL**: http://localhost:8080 (настраивается через `WMS_CORE_API_BASE`)

## Безопасность
Конфигурация безопасности пока открытая (permitAll). Когда будут параметры Keycloak, добавим OIDC-конфигурацию и ограничения по ролям.

## API

### Импорт и приходы
- `POST /api/imports` — импорт XML payload в Черновик (idempotent по messageId)
- `POST /api/receipts` — ручное создание Черновика
- `POST /api/receipts/{id}/confirm` — перевод в Подтвержден
- `POST /api/receipts/{id}/accept` — перевод в Принят
- `GET /api/receipts` / `GET /api/receipts/{id}` — просмотр

### Задания и сканирование ✨ (НОВОЕ)
- `GET /api/tasks` — получить список заданий
- `GET /api/tasks/{id}` — получить задание по ID
- `POST /api/tasks/{id}/scans` — записать скан (требует: palletCode, barcode, qty)
- `GET /api/tasks/{id}/scans` — получить историю сканов задания
- `POST /api/tasks/{id}/assign` — назначить задание
- `POST /api/tasks/{id}/start` — начать выполнение
- `POST /api/tasks/{id}/complete` — завершить задание
- `POST /api/tasks/{id}/release` — освободить задание

**Пример записи скана:**
```bash
curl -X POST http://localhost:8080/api/tasks/3/scans \
  -H "Content-Type: application/json" \
  -d '{"palletCode":"PLT-TEST-001","barcode":"SKU-TEST-002","qty":10}' \
  -u testuser:password
```

**Swagger UI**: http://localhost:8080/swagger-ui.html

## Схема БД
Flyway миграции размещены в `core-api/src/main/resources/db/migration` и создают основные сущности:
- receipts, receipt_lines — приходы и строки
- tasks, scans — задания и сканы
- pallets, locations, zones — паллеты и топология склада
- skus, discrepancies — товары и расхождения
- import_log, receipt_status_history — журналирование

**ER-диаграмма**: [docs/er-diagram.mmd](docs/er-diagram.mmd) | [docs/er-diagram.pdf](docs/er-diagram.pdf)

## Документация

### Для оператора терминала
- 📖 [TERMINAL_USER_GUIDE.md](TERMINAL_USER_GUIDE.md) — полное руководство оператора
- ⚡ [QUICKSTART_TERMINAL.md](QUICKSTART_TERMINAL.md) — быстрый старт за 30 секунд
- 🔧 [PALLET_ERROR_FIX.md](PALLET_ERROR_FIX.md) — решение проблем с паллетами

### Для разработчиков
- 🏗️ [AGENTS.md](AGENTS.md) — архитектура проекта и гайдлайны
- 📋 [docs/TERMINAL_TESTING_CHECKLIST.md](docs/TERMINAL_TESTING_CHECKLIST.md) — чеклист тестирования (80+ тестов)
- ✅ [STATUS.md](STATUS.md) — текущее состояние системы

### Для QA
- 🧪 [TESTING_GUIDE.md](TESTING_GUIDE.md) — руководство по тестированию
- 📊 [START_HERE.md](START_HERE.md) — статус тестовых данных

## Возможности терминала приёмки

- ✅ Сканирование товара с паллет
- ✅ Автоматическая инициализация паллет
- ✅ Накопление количества на паллетах
- ✅ Детекция расхождений (неверный баркод, превышение количества)
- ✅ Запрет смешанных SKU на одной паллете
- ✅ Анимации успеха/ошибки
- ✅ Автофокус и автопереход по Enter
- ✅ История сканов с отметкой расхождений

## Статус проекта

✅ **Backend**: Полностью функционален (Core API + PostgreSQL)  
✅ **Frontend**: Desktop Client с терминалом приёмки  
✅ **Тестовые данные**: Загружены и корректны  
✅ **Документация**: Полная  
⚠️ **Тесты**: 0% coverage (требуется разработка)

**Версия**: 1.0.0 (стабильная)  
**Последнее обновление**: 8 января 2026
