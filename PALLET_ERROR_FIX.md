# 🔧 Исправление проблемы с паллетами

## Проблема
При открытии экрана "Паллеты" появляется ошибка:
```
Ошибка: java.lang.RuntimeException: java.io.IOException: Unexpected status: 500
```

## Причина
Core API был остановлен или упал.

## Решение

### Вариант 1: Перезапуск через batch-файл (рекомендуется)

1. Закрыть окно Core API (если открыто)
2. Дважды кликнуть на файл:
```
start-core-api.bat
```
3. Подождать 10-15 секунд, пока появится:
```
Started CoreApiApplication in X.XXX seconds
```
4. В Desktop Client нажать кнопку **"Обновить"** на экране "Паллеты"

### Вариант 2: Перезапуск всего окружения

Если проблема не решилась:

1. Дважды кликнуть:
```
stop-environment.bat
```
2. Подождать 5 секунд

3. Дважды кликнуть:
```
start-environment.bat
```
4. Подождать 20 секунд

5. Перезапустить Desktop Client

### Вариант 3: Ручная проверка

Открыть командную строку (cmd) и выполнить:

```bash
# Проверить, запущен ли Core API
curl http://localhost:8080/actuator/health
```

**Ожидаемый результат:**
```json
{"status":"UP"}
```

**Если ошибка:**
```
Failed to connect to localhost port 8080
```
Значит Core API не запущен → использовать Вариант 1.

## Профилактика

### Причины падения Core API:
1. **Ошибка в коде** - исправлена в последней версии
2. **Нехватка памяти** - перезапустить систему
3. **Порт 8080 занят** - освободить порт:
```bash
# Найти процесс на порту 8080
netstat -ano | findstr :8080

# Убить процесс (заменить PID на найденный номер)
taskkill //F //PID <PID>
```

### Как избежать проблемы:
- Не закрывать окно Core API во время работы
- Использовать `stop-environment.bat` для остановки всех сервисов
- Проверять логи Core API на наличие ошибок (красный текст)

## Текущий статус

✅ **Core API запущен и работает**
✅ **Все исправления применены**
✅ **Сканирование работает корректно**

Можно продолжать тестирование терминала приёмки.

## Дополнительная информация

См. полную документацию:
- `TERMINAL_USER_GUIDE.md` - руководство оператора
- `QUICKSTART_TERMINAL.md` - быстрый старт
