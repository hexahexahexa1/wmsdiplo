# 📱 Руководство оператора терминала приёмки WMS

## 🎯 Назначение

Терминал приёмки предназначен для сканирования товара при его физической приёмке на складе. Оператор сканирует штрих-коды, указывает паллеты и фиксирует количество принятого товара.

---

## 🚀 Запуск системы

### 1. Запуск окружения

**Вариант А: Автоматический запуск всего**
```cmd
Дважды кликнуть: start-environment.bat
```

**Вариант Б: Запуск вручную**
```cmd
# 1. Запустить PostgreSQL
start-postgres.bat

# 2. Запустить Core API (подождать 10 сек)
start-core-api.bat

# 3. Запустить Desktop Client
start-desktop-client.bat
```

### 2. Вход в систему

При запуске Desktop Client откроется окно авторизации:
- **Логин**: `testuser`
- **Пароль**: `password`
- Нажать кнопку **"Войти"**

---

## 📋 Процесс приёмки (пошаговая инструкция)

### ШАГ 1: Открыть терминал приёмки

1. После входа в главном меню слева найти кнопку **"Терминал"**
2. Кликнуть на неё

**Результат**: Откроется экран с двумя вкладками:
- **"Мои задачи"** - задания, назначенные мне
- **"Все задачи"** - все задания в системе (только просмотр)

### ШАГ 2: Выбрать задание на приёмку

В таблице "Мои задачи" отображаются столбцы:
- **ID** - номер задания
- **Тип** - тип задания (RECEIVING = приёмка)
- **Статус** - статус (IN_PROGRESS = выполняется)
- **Приход** - номер документа прихода
- **Кол-во** - прогресс: `выполнено / назначено`

**Действие**: 
- **Дважды кликнуть** на любую строку с заданием

**Результат**: Откроется диалоговое окно "Задание #N" с двумя вкладками

### ШАГ 3: Изучить документ (вкладка "Документ")

На вкладке **"Документ"** отображается информация о задании:
- Номер прихода
- Тип задания
- Статус
- Ожидаемое количество
- Текущий исполнитель

Эта информация только для просмотра.

### ШАГ 4: Начать сканирование (вкладка "Факт")

Переключиться на вкладку **"Факт"**

**Экран содержит:**

#### 🔵 Поля сканирования (синие, крупные, обязательные):
1. **Паллета** - код паллеты (например, `PLT-TEST-001`)
2. **Баркод** - штрих-код товара (например, `SKU-TEST-002`)

#### ⚪ Поля ввода (белые, обычные):
3. **Количество** - принятое количество (целое число, например `10`)
4. **Комментарий** - текстовое поле для заметок (опционально)

#### Кнопка:
5. **"Отправить"** - зафиксировать скан

#### Таблица сканов:
Внизу отображается история всех сканов для этого задания.

### ШАГ 5: Ввести данные скана

#### Порядок работы (с автопереходом по Enter):

**1. Паллета** 
- Сканировать или ввести код паллеты: `PLT-TEST-001`
- Нажать **Enter** → курсор переходит в поле "Баркод"

**2. Баркод**
- Сканировать или ввести штрих-код: `SKU-TEST-002`
- Нажать **Enter** → курсор переходит в поле "Количество"

**3. Количество**
- Ввести количество: `10`
- Нажать **Enter** → **АВТОМАТИЧЕСКАЯ ОТПРАВКА**

**4. Комментарий** (опционально)
- Можно оставить пустым
- Или написать замечание

### ШАГ 6: Наблюдать результат

После нажатия Enter в поле "Количество" произойдёт:

#### ✅ Успешный скан:
- **Зелёная вспышка** (fade in/out анимация)
- Все поля очищаются
- Курсор возвращается в поле "Паллета"
- Новая запись появляется в таблице сканов
- Прогресс задания обновляется

#### ❌ Ошибка валидации:
- **Красная тряска** (shake анимация)
- Сообщение об ошибке внизу формы
- Поля НЕ очищаются (можно исправить данные)

### ШАГ 7: Продолжить сканирование

Повторять ШАГ 5-6 до полного выполнения задания.

**Можно использовать:**
- Одну и ту же паллету для нескольких сканов
- Разные паллеты для одного задания
- Любое количество за один скан (в пределах остатка)

### ШАГ 8: Закрыть задание

Когда всё количество принято:
1. Закрыть диалог кнопкой **X** или **ESC**
2. Вернуться к списку заданий
3. Выбрать следующее задание

---

## 🎯 Тестовые данные

### Доступные учётные данные
```
Логин: testuser
Пароль: password
```

### Задания для testuser

| ID | Приход | Ожидается | Выполнено | Статус |
|----|--------|-----------|-----------|--------|
| 3  | ACT-TEST-2026-001 | 50 | 0 | IN_PROGRESS |
| 4  | ACT-TEST-2026-002 | 200 | 75 | IN_PROGRESS |

### Доступные паллеты
```
PLT-TEST-001 (пустая)
PLT-TEST-002 (пустая)
PLT-TEST-003 (используется, SKU-TEST-002, qty=75)
PLT-TEST-004 (пустая)
PLT-TEST-005 (пустая)
```

### Баркоды товаров (SKU)
```
SKU-TEST-001
SKU-TEST-002
SKU-TEST-003
```

---

## ✅ Примеры сценариев

### Пример 1: Простой скан (Happy Path)

**Задание**: #3, ожидается SKU-TEST-002

**Действия:**
1. Открыть задание #3 (двойной клик)
2. Переключиться на вкладку "Факт"
3. Ввести данные:
   - Паллета: `PLT-TEST-001` → Enter
   - Баркод: `SKU-TEST-002` → Enter
   - Количество: `10` → Enter (автоотправка)

**Результат:**
- ✅ Зелёная вспышка
- Запись в таблице: PLT-TEST-001, SKU-TEST-002, 10 шт
- Прогресс: 10 / 50

### Пример 2: Несколько сканов на одну паллету

**Задание**: #3

**Действия:**
1. Скан 1:
   - Паллета: `PLT-TEST-001`
   - Баркод: `SKU-TEST-002`
   - Количество: `10` → Enter
   
2. Скан 2 (та же паллета):
   - Паллета: `PLT-TEST-001`
   - Баркод: `SKU-TEST-002`
   - Количество: `15` → Enter

**Результат:**
- ✅ Оба скана успешны
- Паллета PLT-TEST-001 содержит 25 единиц SKU-TEST-002
- Прогресс задания: 25 / 50

### Пример 3: Продолжение частично выполненного задания

**Задание**: #4 (уже выполнено 75/200)

**Действия:**
- Паллета: `PLT-TEST-003` (уже используется)
- Баркод: `SKU-TEST-002`
- Количество: `20` → Enter

**Результат:**
- ✅ Успешный скан
- Паллета PLT-TEST-003 теперь содержит 95 единиц
- Прогресс: 95 / 200
- В таблице появится 4 скана (3 старых + 1 новый)

### Пример 4: Ошибка - пустая паллета

**Действия:**
- Паллета: _(оставить пустым)_
- Баркод: `SKU-TEST-002`
- Количество: `10` → Enter

**Результат:**
- ❌ Красная тряска
- Сообщение: **"Код паллеты обязателен"**
- Поля не очищаются, можно исправить

### Пример 5: Ошибка - неверный баркод

**Задание**: #3 (ожидается SKU-TEST-002)

**Действия:**
- Паллета: `PLT-TEST-002`
- Баркод: `WRONG-BARCODE`
- Количество: `5` → Enter

**Результат:**
- ⚠️ Скан ЗАПИСАН, но с отметкой расхождения
- В таблице в колонке "Расхождение" будет значение **"BARCODE_MISMATCH"**
- Прогресс обновится: 5 / 50
- Создаётся запись discrepancy в системе

### Пример 6: Попытка смешать SKU на одной паллете

**Паллета PLT-TEST-001 уже содержит** SKU-TEST-002

**Действия:**
- Паллета: `PLT-TEST-001`
- Баркод: `SKU-TEST-001` _(другой товар!)_
- Количество: `10` → Enter

**Результат:**
- ❌ Красная тряска
- Сообщение: **"Паллета PLT-TEST-001 уже содержит другой SKU. Смешанные паллеты запрещены."**
- Скан НЕ записан

---

## ⚙️ Особенности работы

### Автоматическая инициализация паллеты

При первом скане на пустую паллету система автоматически:
1. Присваивает паллете SKU из задания
2. Устанавливает статус `RECEIVING`
3. Размещает паллету в транзитную локацию (RECEIVING зона)
4. Инициализирует количество нулём

### Запрет на смешанные SKU

Одна паллета может содержать только один SKU. 
- Если паллета уже инициализирована с SKU-TEST-001, на неё нельзя сканировать SKU-TEST-002
- Используйте другую пустую паллету

### Накопление количества

Количество на паллете **накапливается**:
- Скан 1: PLT-001 + 10 шт → паллета = 10
- Скан 2: PLT-001 + 15 шт → паллета = 25
- Скан 3: PLT-001 + 5 шт → паллета = 30

### Обновление прогресса задания

Поле `qtyDone` задания обновляется при каждом скане:
- Текущий прогресс + новое количество = новый прогресс
- Отображается в списке заданий как `75 / 200`

### Детекция расхождений

Система автоматически создаёт записи discrepancies при:
1. **BARCODE_MISMATCH** - отсканированный баркод не совпадает с ожидаемым SKU.code
2. **OVER_QTY** - принято больше, чем ожидалось (qtyDone > qtyExpected)
3. **SSCC_MISMATCH** - SSCC не совпадает с ожидаемым (если указан)

---

## 🐛 Типичные ошибки и решения

### Ошибка: "Код паллеты обязателен"
**Причина**: Поле "Паллета" пустое  
**Решение**: Ввести код паллеты

### Ошибка: "Pallet not found"
**Причина**: Паллета с таким кодом не существует в БД  
**Решение**: Проверить код паллеты, использовать PLT-TEST-001 до PLT-TEST-005

### Ошибка: "Receipt is not in receiving state"
**Причина**: Приход имеет статус отличный от IN_PROGRESS или PENDING_RESOLUTION  
**Решение**: Обратиться к администратору для изменения статуса прихода

### Ошибка: "Mixed-SKU pallets are not allowed"
**Причина**: Попытка сканировать товар А на паллету, которая уже содержит товар Б  
**Решение**: Использовать другую пустую паллету

### Ошибка: "Internal Server Error (500)"
**Причина**: Core API не запущен или упал  
**Решение**: Перезапустить Core API:
```cmd
start-core-api.bat
```

### Не вижу задания в терминале
**Причина**: Заданий нет или они назначены другому пользователю  
**Решение**: 
1. Убедиться, что вошли как `testuser`
2. Нажать кнопку "Обновить"
3. Проверить в БД:
```sql
SELECT id, assignee, status FROM tasks WHERE assignee='testuser';
```

### Таблица сканов пустая
**Причина**: Для задания ещё не было сканов  
**Решение**: Это нормально для новых заданий. После первого скана появится запись.

---

## 🔧 Техническая информация

### Системные требования
- Java 17+
- PostgreSQL 16 (в Docker)
- Windows 10/11
- Свободный порт 8080 (Core API)
- Свободный порт 55432 (PostgreSQL)

### Архитектура
```
Desktop Client (JavaFX)
    ↓ REST API
Core API (Spring Boot :8080)
    ↓ JDBC
PostgreSQL (Docker :55432)
```

### Основные endpoints

**Авторизация:**
```
GET /api/users/me
Authorization: Basic dGVzdHVzZXI6cGFzc3dvcmQ=
```

**Получить задания:**
```
GET /api/tasks
Authorization: Basic testuser:password
```

**Записать скан:**
```
POST /api/tasks/{id}/scans
Content-Type: application/json
{
  "palletCode": "PLT-TEST-001",
  "barcode": "SKU-TEST-002",
  "qty": 10,
  "comment": "Опциональный комментарий"
}
```

**Получить сканы задания:**
```
GET /api/tasks/{id}/scans
```

### Структура данных скана (ScanDto)
```json
{
  "id": 14,
  "taskId": 3,
  "palletCode": "PLT-TEST-001",
  "sscc": null,
  "barcode": "SKU-TEST-002",
  "qty": 10,
  "deviceId": null,
  "discrepancy": false,
  "scannedAt": "2026-01-08T15:50:37.4768744"
}
```

---

## 📞 Поддержка

### Логи системы

**Core API:**
- Консоль окна `start-core-api.bat`
- Лог уровень: DEBUG для `com.wmsdipl.core`

**Desktop Client:**
- Консоль окна `start-desktop-client.bat`
- Ошибки отображаются в UI красным текстом

### Проверка здоровья API
```bash
curl http://localhost:8080/actuator/health
# Ответ: {"status":"UP"}
```

### Проверка подключения к БД
```bash
docker exec -it wmsdipl-postgres psql -U wmsdipl -d wmsdipl
```

### Перезапуск окружения
```cmd
# Остановить всё
stop-environment.bat

# Запустить заново
start-environment.bat
```

---

## 📚 Дополнительная документация

- **Полный чеклист тестирования**: `docs/TERMINAL_TESTING_CHECKLIST.md`
- **Руководство по тестированию**: `TESTING_GUIDE.md`
- **Быстрый старт**: `START_HERE.md`
- **Структура проекта**: `AGENTS.md`
- **SQL тестовых данных**: `test-data-final.sql`

---

## ✨ Готово к работе!

Система полностью функциональна и готова к приёмке товара.

**Начните с простого скана:**
1. Войти как `testuser` / `password`
2. Открыть "Терминал"
3. Дважды кликнуть на задание #3
4. Вкладка "Факт"
5. PLT-TEST-001 → SKU-TEST-002 → 10 → Enter
6. Увидеть зелёную вспышку ✅

**Удачной приёмки!** 🚀
