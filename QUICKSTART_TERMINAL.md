# ⚡ Быстрый старт - Терминал приёмки

## 🚀 За 30 секунд

### 1. Запуск (дважды кликнуть)
```
start-environment.bat
```
Подождать 15 секунд.

### 2. Вход
- **Логин**: `testuser`
- **Пароль**: `password`
- Нажать **"Войти"**

### 3. Открыть терминал
- Кликнуть **"Терминал"** в меню слева

### 4. Выбрать задание
- **Дважды кликнуть** на любую строку в таблице

### 5. Сканировать
- Вкладка **"Факт"**
- **Паллета**: `PLT-TEST-001` → Enter
- **Баркод**: `SKU-TEST-002` → Enter
- **Количество**: `10` → Enter
- **Результат**: Зелёная вспышка ✅

---

## 📋 Шпаргалка

### Тестовые данные
| Что | Значение |
|-----|----------|
| Логин | `testuser` |
| Пароль | `password` |
| Паллеты | `PLT-TEST-001`, `PLT-TEST-002`, `PLT-TEST-004`, `PLT-TEST-005` |
| Баркоды | `SKU-TEST-001`, `SKU-TEST-002`, `SKU-TEST-003` |

### Задания
| ID | Приход | Прогресс |
|----|--------|----------|
| 3 | ACT-TEST-2026-001 | 0 / 50 |
| 4 | ACT-TEST-2026-002 | 75 / 200 |

### Горячие клавиши
- **Enter** в поле "Паллета" → переход в "Баркод"
- **Enter** в поле "Баркод" → переход в "Количество"
- **Enter** в поле "Количество" → отправка скана
- **ESC** → закрыть диалог

### Анимации
- **Зелёная вспышка** = успех ✅
- **Красная тряска** = ошибка ❌

---

## 🐛 Частые проблемы

| Проблема | Решение |
|----------|---------|
| Desktop Client не открывается | Проверить, что Core API запущен |
| Ошибка 500 | Перезапустить: `start-core-api.bat` |
| Нет заданий | Нажать "Обновить" |
| Ошибка "Pallet not found" | Использовать `PLT-TEST-001` до `PLT-TEST-005` |
| Ошибка "Mixed SKU" | Взять другую пустую паллету |

---

## ✅ Проверка работы

**Шаг 1**: Проверить Core API
```bash
curl http://localhost:8080/actuator/health
# Должен вернуть: {"status":"UP"}
```

**Шаг 2**: Проверить задания
```bash
curl -u testuser:password http://localhost:8080/api/tasks
# Должен вернуть JSON с заданиями
```

**Шаг 3**: Тестовый скан через API
```bash
curl -X POST http://localhost:8080/api/tasks/3/scans \
  -H "Content-Type: application/json" \
  -d '{"palletCode":"PLT-TEST-001","barcode":"SKU-TEST-002","qty":10}' \
  -u testuser:password
# Должен вернуть HTTP 201 Created
```

---

## 📖 Полная документация
Смотри: `TERMINAL_USER_GUIDE.md`

---

**Готово к работе!** 🎉
